---
import type { CollectionEntry } from 'astro:content';
import { getTechnologyColor } from '../utils/technologies';

interface Props {
  project: CollectionEntry<'work'>;
}

const { project } = Astro.props;
const technologies = Object.values(project.data.technologies || {}).flat();
---

<a class="work-card" href={`/work/${project.id}`} data-id={project.id}>
  <h3 class="title">{project.data.title}</h3>
  <p class="description">{project.data.description}</p>
  <div class="tags">
    {project.data.fields.map(tag => (
      <span class="tag">{tag}</span>
    ))}
  </div>
  <div class="tags">
    {technologies.map(tech => (
      <span class="tag tech" style={`--tech-bg: ${getTechnologyColor(tech)}`}>{tech}</span>
    ))}
  </div>
</a>

<style>
  .work-card {
    border: 1px solid var(--gray-800);
    border-radius: 1rem;
    padding: 1.5rem;
    background-color: var(--gray-999);
    transition: box-shadow var(--theme-transition), transform var(--theme-transition);
    box-shadow: var(--shadow-sm);
    display: flex;
    flex-direction: column;
    /* Reset anchor default link styles so children don't appear underlined */
    text-decoration: none;
    color: inherit;
    gap: 0.5rem;
  }

  .work-card:hover {
    box-shadow: var(--shadow-md);
    transform: translateY(-2px);
  }

  .work-card:focus-visible {
    outline: 3px solid var(--accent-regular);
    outline-offset: 4px;
    border-radius: 1rem;
  }

  .work-card .title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--gray-0);
  }

  .work-card .description {
    color: var(--gray-300);
    margin-bottom: 0.5rem;
    font-size: var(--text-sm);
    flex-grow: 1;
  }

  .work-card .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .work-card .tag {
    background-color: var(--gray-800);
    color: var(--gray-200);
    font-size: 0.75rem;
    font-weight: 500;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
  }

  .work-card .tech {
    color: #ffffff;
    background-color: color-mix(in srgb, var(--tech-bg) 80%, var(--gray-600));
  }

  .theme-dark .work-card .tag {
    background-color: var(--gray-700);
  }

</style>